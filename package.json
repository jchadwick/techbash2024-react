{
  "scripts": {
    "start": "run-p -l json:server start:demo start:react-app",
    "start:demo": "live-server --port=3001 --open=/ ./demo",
    "start:react-app": "live-server --port=3002 --open=/ ./react-app",
    "json:server": "json-server db.json"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.2",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
